void main() {
  /**
   * SYMBOL
   * Symbol merupakan tipe data yang jarang digunakan,
   * Symbol sebenernya bisa digunkaan sebagai constant,
   * untuk membuat Symbol, kita bisa menggunakan tanda #, atau jiga kita butuh membuat symbol dengan nama yang mengandung spasi, kita bisa menggunakan Symbol("text")
   */
  Symbol symbol1 = Symbol('Muhammad Sulthon Mufti');
  var symbol2 = #belajar_dart;

  print(symbol1);
  print(symbol2);
}
